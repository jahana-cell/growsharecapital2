import React from 'react';

export const metadata = {
  title: "Secure Checkout | GrowShare Capital",
  description: "Complete your purchase securely.",
};

export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
